import React from "react"
import PropTypes from "prop-types"
class M_btn extends React.Component {
  render () {
    return (

        <button className={'M_btn_primary'}>
          КНОПКА
        </button>

    );
  }
}

export default M_btn
